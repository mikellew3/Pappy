{
  "name": "pappy-sync-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker + KV sync endpoint for Pappy & One.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
